# User Service - Docker env
# Use docker hostnames when running in Docker Compose
DATABASE_URL=postgresql://postgres:root123@postgres:5432/growup
REDIS_URL=redis://redis:6379
AUTH_SERVICE_URL=http://auth-service:3001
PORT=3002
NODE_ENV=development

# Postgres connection (if libraries use individual vars)
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=growup
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root123

# CORS and uploads
CORS_ORIGIN=http://localhost:3000
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

LOG_LEVEL=info